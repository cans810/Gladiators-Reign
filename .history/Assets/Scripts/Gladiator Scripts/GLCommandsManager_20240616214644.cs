using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GLCommandsManager : MonoBehaviour
{
    public bool can_arrange_fight;
    public bool can_sendto_training;
    public bool can_manage_equipment;
    public bool can_manage_abilities;
    public bool can_manage_magicskills;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
